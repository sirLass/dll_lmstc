services:
  - type: web
    name: dll-lmstc
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn DLL_LMSTC.wsgi:application --bind 0.0.0.0:10000
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: True
      - key: ALLOWED_HOSTS
        value: "dll-lmstc-gza7.onrender.com"
      - key: RENDER
        value: True
      - key: DATABASE_URL
        value: "postgresql://postgres:LMSTC_admin01@db.tfxdcasngsmkittftsgr.supabase.co:5432/postgres"
